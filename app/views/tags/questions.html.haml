.row
  .span2
  .span8
    .page-header 
      %h1 Filter questions by #{params[:tag]}
    .row
      .span8
        #tag_cloud
          - tag_cloud Question.tag_counts, %w(label) do |tag, css_class|
            = tag_questions_link(tag, css_class)
        %hr

    = render '/questions/question_feed'

    = link_to 'New Question', new_question_path